# Deployment Engine

Before deployment:

- confirm backups;
- confirm environment configuration;
- confirm database migration requirements;
- confirm PHP/WordPress compatibility;
- confirm file permissions;
- confirm URLs;
- confirm caching;
- confirm HTTPS;
- confirm critical forms/integrations;
- confirm administrator access.

Never assume local configuration can be copied directly to production.
