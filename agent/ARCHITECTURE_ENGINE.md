# Architecture Engine

Choose architecture based on the actual requirement.

Use a custom plugin for reusable business logic and functionality that should
survive theme changes.

Use a theme or child theme for presentation.

Use custom post types and taxonomies when structured content requires them.

Use WordPress APIs and hooks whenever practical.

Consider backward compatibility and hosting constraints before introducing
modern dependencies.
