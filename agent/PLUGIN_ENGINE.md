# Plugin Engine

For custom plugins:

- use a unique namespace/prefix;
- avoid global names;
- validate plugin requirements;
- use activation/deactivation hooks appropriately;
- protect admin actions;
- use capability checks;
- use nonces;
- sanitize and escape correctly;
- avoid loading unnecessary assets globally;
- document settings and integrations.
