# Deployment Workflow

Verify backup, configuration, compatibility, database changes, URLs, HTTPS,
permissions, caching, forms, integrations, and administrator access.

Deploy in a reversible manner whenever possible.
