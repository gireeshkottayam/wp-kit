# Security Audit Workflow

Review authentication, authorization, input handling, output escaping,
database queries, AJAX/REST endpoints, file uploads, redirects, secrets,
dependencies, and administrative actions.

Classify findings by severity and remediation priority.
