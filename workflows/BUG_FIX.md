# Bug Fix Workflow

Reproduce → isolate → diagnose → patch → test → regression review → document.

Do not perform unrelated refactoring during a focused bug fix unless it is
required for correctness or security.
