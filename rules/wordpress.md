# WordPress Rules

Use WordPress APIs and hooks wherever practical.

Never modify core files as a normal implementation.

Use unique prefixes/namespaces for custom code.

Use enqueue APIs for assets.

Respect roles and capabilities.

Use the Settings API for substantial plugin settings.

Use translation functions for user-facing strings where internationalization
is relevant.
