# HTML Rules

Use semantic HTML.

Provide accessible names and labels.

Use headings in a logical hierarchy.

Do not use non-semantic elements as interactive controls when native
elements are appropriate.
