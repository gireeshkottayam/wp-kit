# JavaScript Rules

Use modular, maintainable JavaScript.

Avoid unnecessary global variables.

Handle failures gracefully.

Do not expose secrets in frontend code.

Prefer progressive enhancement when appropriate.
