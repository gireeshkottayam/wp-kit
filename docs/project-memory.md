# Project Memory

Project memory lives under `.wp-kit/`.

It should describe the current state of the project rather than become a
duplicate copy of source code.

Update memory when architecture, requirements, security posture, major
features, or development state changes.
